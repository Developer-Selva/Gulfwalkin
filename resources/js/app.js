import './bootstrap';

import Alpine from 'alpinejs';
import 'bootstrap';
import { Tooltip, Toast, Popover } from 'bootstrap';
import $ from 'jquery';  
window.$ = $; 

window.Alpine = Alpine;

Alpine.start();
